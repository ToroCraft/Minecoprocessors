package net.torocraft.minecoprocessors.blocks;

import java.util.Arrays;
import org.junit.Assert;
import org.junit.Test;

public class TileEntityMinecoprocessorTest {

  @Test
  public void testNameParser() {

    // @todo: RE-ENABLE
    //Assert.assertNull(MinecoprocessorTileEntity.readNameFromHeader(Arrays.asList("")));
    //Assert.assertNull(MinecoprocessorTileEntity.readNameFromHeader(Arrays.asList(" ")));
    //Assert.assertNull(MinecoprocessorTileEntity.readNameFromHeader(Arrays.asList("asdf")));
    //Assert.assertNull(MinecoprocessorTileEntity.readNameFromHeader(Arrays.asList(";  ", " foo code ", " ", "")));
    //Assert.assertEquals("test title", MinecoprocessorTileEntity.readNameFromHeader(Arrays.asList("; test title ", " foo code ", " ", "")));

    //TODO fix test or code
    //Assert.assertNull(TileEntityMinecoprocessor.readNameFromHeader(null));
  }
}